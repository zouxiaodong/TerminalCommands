#include <iostream>

int main(int argc, char* argv[]) {
    // Check if there are two parameters passed in
    // Throw error if not
    /*
     * TODO:
     * - Check if there are two parameters passed.
     *  - Throw error if this is false
     * - If this condition passes then:
     *  -
     */
}
